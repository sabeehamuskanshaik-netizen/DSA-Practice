import java.util.*;
class Solution {
    public int maxSubArray(int[] nums) {
     int ans=nums[0];
     int max=nums[0];
     for(int i=1;i<nums.length;i++){
        max=Math.max(nums[i],max+nums[i]);
        if(max>ans){
            ans=max;
        }
     }  
     return ans;
    }
}
